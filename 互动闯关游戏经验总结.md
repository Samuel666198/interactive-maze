# 闯关答题迷宫迭代总结（2026-06-19）

## 本次目标

围绕 README 中的扩展方向，对课堂互动答题迷宫做功能增强和性能优化，同时保持“温润冒险手作感”的视觉风格，不削减美术表现。

## 功能扩展

### 1. 地图与角色增强

- 增加地图地形装饰：草地、石板、宝箱、障碍物。
- 学生加入前可选择角色头像。
- 头像最终改为 emoji 图标形式：`🎨`、`🚀`、`⭐`、`👑`、`💎`、`🧭`。
- 地图棋子和排行榜同步显示角色头像。

### 2. 交互布局调整

- 将“开始闯关”和“加入游戏”整合到同一个底部控制卡片。
- 顶部工具栏只保留教师模式、题库导入、课堂统计、玩法说明、复位等管理操作。
- 中间等待态重新排版，改为居中显示“准备就绪”和引导文案。
- 修复等待态、进行态、结束态之间的 class 切换。

### 3. 课堂统计与多轮记录

- 新增“课堂统计”面板。
- 统计参与学生数、答题次数、班级正确率。
- 记录每位学生得分、答题数、正确率。
- 通关后自动写入多轮比赛记录。
- 支持清空多轮记录。

### 4. 题库与配置

- 题库导入面板增加“下载题库模板 JSON”。
- 教师模式增加本地配置保存、读取、清除。
- 保存内容包括限时、起点、终点、题库、学生名单、多轮记录等。
- 使用 `localStorage`，不依赖后端。

### 5. 启动脚本

- 更新 `start-server.bat`。
- 启动前先清理旧的 `8018` 端口服务。
- 打开页面时追加随机 `?v=` 参数，避免浏览器缓存导致新内容不显示。

## 性能优化

### 1. Worker 多线程改造

新增 `scripts/maze-worker.js`。

Worker 负责：

- 生成随机迷宫路线。
- 生成地形数据。
- 在支持 `OffscreenCanvas` 的浏览器中后台预渲染地图底图。

主线程现在主要负责：

- UI 交互。
- 学生棋子叠加绘制。
- 答题流程控制。

如果浏览器不支持 Worker 或 OffscreenCanvas，会自动回退到主线程 Canvas 绘制，保证可用性。

### 2. Canvas 与合成优化

- 主线程优先使用 Worker 返回的 `ImageBitmap` 绘制地图底图。
- Canvas context 使用 `desynchronized: true`。
- 对 Canvas、选项按钮、计时环增加 `contain`，限制重绘范围。
- 保留地图纹理、地形、路线、起终点标记等美术效果。

### 3. 音频优化

原问题：

- 每次答题都创建新的 `AudioContext`，可能造成交互瞬间卡顿。

优化后：

- 全局复用一个 `AudioContext`。
- 使用 master gain 管理音量。
- 在用户点击“开始闯关”、选择答案、提交答案时提前预热音频上下文。

### 4. 动效优化

原问题：

- `adventure-effects.js` 使用 `getBoundingClientRect()` 计算弹字位置，会触发布局读取。

优化后：

- 改为 viewport 锚点。
- viewport 尺寸只在初始化和 `resize` 时更新。
- 答题反馈时不再读取布局。

### 5. INP 优化

性能面板显示本地 INP 曾达到约 `344ms`，交互目标为 pointer。

处理方式：

- 点击选项时不再同步 `querySelectorAll` 扫描所有选项。
- pointer 回调只处理当前被点击选项。
- 正确答案高亮拆到下一帧。
- 后续答题判定通过延迟任务执行。
- 增加 `answerLocked`，防止重复点击。
- 去掉 hover 中的动态 `filter` 和大阴影变化，保留静态美术阴影。

## Trace 分析结论

### 第一份 Trace

文件：

- `Trace-20260619T195622.json.gz`

观察：

- 大量红色斜纹集中在帧轨道。
- 主线程没有持续大块长任务。
- `VizCompositorThread` 有大量约 77-83ms 的任务。
- 初步判断瓶颈偏合成、截图、光栅，而不是普通 JS 计算。

结论：

- DevTools 开启“屏幕截图”会显著放大掉帧。
- 页面中透明层、阴影、模糊、Canvas、动效会给合成线程增加压力。

### 第二份 Trace

文件：

- `Trace-20260619T202219.json.gz`

观察：

- Worker 已生效，出现 DedicatedWorker 线程。
- `maze-worker.js` 单次计算约 `2.88ms`。
- 主线程出现一次约 `197ms` 的 `FireAnimationFrame / FunctionCall`。
- 对应代码位置是点击选项后的 `deferWork(() => handleAnswer(...), 600)`。

结论：

- Worker 解决了路线和地图底图生成问题。
- 后续瓶颈转移到答题点击后的交互链路。
- 将重任务放入 `requestAnimationFrame` 是错误方向，会占用关键渲染帧。

后续处理：

- `handleAnswer` 不再放入 rAF。
- 音频提前预热。
- 动效锚点缓存。
- 选项点击路径拆分，降低 INP。

## 踩坑点

### 1. README 在终端里出现乱码

PowerShell 默认输出编码导致中文显示乱码。

处理方式：

- 使用 `Get-Content -Encoding UTF8` 读取。
- 后续编辑保持 UTF-8。

### 2. 启动脚本打开的不是新版页面

原因：

- 旧的 `8018` 服务还在运行。
- 浏览器缓存旧页面。

处理方式：

- `start-server.bat` 启动前杀掉旧端口进程。
- URL 追加随机 `?v=` 参数。

### 3. Worker 在 file:// 下不稳定

Worker 需要通过本地服务器加载，直接打开 `index.html` 可能不工作。

处理方式：

- 推荐使用 `start-server.bat` 或 `python -m http.server 8018`。
- README 中保留本地服务器说明。

### 4. OffscreenCanvas 不能假设一定可用

不是所有浏览器环境都支持 Worker 内 OffscreenCanvas。

处理方式：

- Worker 返回 `hasBitmap`。
- 不支持时主线程自动回退到原 Canvas 绘制。

### 5. rAF 不适合塞重任务

错误做法：

- 把 `handleAnswer` 包进 `requestAnimationFrame`，以为能更顺滑。

实际结果：

- Trace 显示一次 rAF 回调达到约 `197ms`。
- 它占用了关键动画帧，反而恶化交互表现。

处理方式：

- rAF 只用于轻量视觉更新。
- 答题判定、计分、移动等逻辑走普通延迟任务或分批任务。

### 6. getBoundingClientRect 会触发布局读取

原动效弹字通过 `getBoundingClientRect()` 找位置。

问题：

- 答题反馈时触发布局同步计算。

处理方式：

- 使用缓存 viewport 锚点。
- 只在 resize 时更新尺寸。

### 7. AudioContext 首次初始化可能卡顿

问题：

- 第一次播放音效时创建/恢复 AudioContext，容易影响交互。

处理方式：

- 用户手势阶段提前预热。
- 后续复用同一个 AudioContext。

### 8. pointer 回调里不能做太多 DOM 操作

原逻辑：

- 点击选项后同步扫描所有选项。
- 逐个写 style 和 class。

问题：

- INP 指标变差。

处理方式：

- 点击时只处理当前选项。
- 正确答案高亮拆到下一帧。
- 父容器加 locked class 防重复点击。

### 9. Node stdin 读取中文路径可能乱码

在 PowerShell 通过 stdin 跑 Node 脚本时，中文路径曾变成问号。

处理方式：

- 将工作目录切到项目目录。
- 用相对路径读取文件。

## 当前文件变更

主要涉及：

- `index.html`
- `README.md`
- `start-server.bat`
- `scripts/adventure-effects.js`
- `scripts/maze-worker.js`

新增文件：

- `scripts/maze-worker.js`

本总结文件：

- `work-summary-2026-06-19.md`

## 验证记录

已做过的验证：

- `index.html` 内联脚本语法解析通过。
- `scripts/adventure-effects.js` 语法解析通过。
- `scripts/maze-worker.js` 语法解析通过。
- Worker 计算分支验证通过。
- 本地服务资源检查通过：
  - `index=200`
  - `worker=200`
  - `fx=200`

推荐后续性能测试方式：

- DevTools Performance 录制时关闭“屏幕截图”。
- DevTools 尽量独立窗口打开。
- 单次只录 5-10 秒。
- 分别观察 Main、Worker、Compositor、Raster、GPU/Viz。
- 对 INP 测试时，专门点击一次选项按钮，看 pointer 交互是否下降。

## 后续建议

如果 INP 仍然偏高，继续拆 `handleAnswer`：

- 第一阶段：只做当前选项反馈。
- 第二阶段：播放音效和触发动效。
- 第三阶段：更新分数和排行榜。
- 第四阶段：移动棋子。
- 第五阶段：进入下一题。

如果合成线程仍然掉帧，继续优化 GPU 合成压力：

- 将部分背景纹理预烘焙为静态 Canvas 或图片。
- 减少运行时 `backdrop-filter` 使用范围。
- 保留视觉效果，但降低大面积透明层叠加。
- 避免 DevTools 截图录制误判真实性能。
